profile: restricted

exclusions:
  namespaces:
    - "kube-system"
  workloadLabels:
    kubeimpact.io/exclude: "true"
  namespaceLabels:
    kubeimpact.io/exclude: "true"

severityOverrides:
  WLK-HOSTNET-001: high
